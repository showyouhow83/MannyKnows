---
# ─────────────────────────────────────────────────────────────────────────────
# CASE STUDY TEMPLATE — duplicate this file to add a new project.
# Keep `draft: true` until EVERY field below is true and filled in. Draft entries
# never build a page (honesty rule — don't publish anything you'd have to invent).
# When it's ready: set draft to false, then add `caseStudy: 'sl-painting'` to the
# matching item in src/data/selectedWork.ts so the homepage card links here.
# ─────────────────────────────────────────────────────────────────────────────
draft: true   # ← unfinished template: do NOT publish. Flip to false when filled in.
title: "SL Painting"
client: "SL Painting"
tagline: "The website and lead-to-job platform that runs a painting contractor's whole operation."
summary: "A painting contractor needed more than a website — they needed one place to catch leads and run every job from estimate to final walkthrough. We built both."
industry: "Painting contractor"

# At a glance ──────────────────────────────────────────────────────────────────
role: "Design, build & deploy"      # TODO: confirm scope wording
# timeline: "TODO — e.g. 4 weeks"
# year: 2025
services:
  - "Lead-gen website"
  - "Lead-to-job platform"
  # TODO: add/trim to what was actually delivered
techStack:
  # TODO: list only what this project actually uses. Examples you might confirm:
  # - "Astro"
  # - "Cloudflare Workers"
  # - "Tailwind CSS"
  # - "Twilio"
  []
goals:
  - "Capture and organize leads instead of losing them to voicemail and texts"
  - "Run estimates, scheduling, and jobs from a single place"
  # TODO: confirm/expand with the client's actual goals

# Outcome metrics — each becomes a stat tile. ONLY real, verifiable numbers.
# Leave empty until you have true figures.
results: []
#  - { value: "TODO", label: "e.g. leads captured / month" }
#  - { value: "TODO", label: "e.g. hours saved / week" }

liveUrl: "https://slpainting.co"
heroImage: "slpainting-desktop"     # responsive variants already in public/works/
# gallery:
#   - "slpainting-desktop"          # add more public/works/ base names for a gallery
---

<!--
  This body is the narrative. Write in plain language, only what's true.
  Suggested structure below — delete any section you can't fill honestly.
-->

## The challenge

<!-- TODO: What was the situation before? What was breaking or being lost?
     e.g. leads coming in by call/text/DM with no system, jobs tracked on paper. -->

## What we built

<!-- TODO: Describe the site + platform in the client's terms. What does it do
     day to day? Website captures leads → pipeline organizes them → jobs get
     scheduled and tracked. Keep it concrete. -->

## How it works

<!-- TODO: The mechanics worth bragging about — quote forms, automatic
     follow-up, one dashboard, crew scheduling, before/after photos, etc.
     Only include features that actually shipped. -->

## The outcome

<!-- TODO: What changed for the business? Tie back to the goals above. If you
     have real numbers, they belong in `results` in the frontmatter too. -->
