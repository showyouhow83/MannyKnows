// Single source of truth for the "Book a call" / booking link.
// This is a Google Appointment Schedule page (Google Calendar) — no API or
// token needed; Google handles availability, reminders, and confirmations.
// To point bookings elsewhere (e.g. a dedicated "discovery call" appointment
// type), change this one value and every "Book a call" button follows.
export const BOOKING_URL = 'https://calendar.app.google/FTYeET27mKVjngB29';
