---
// Local SEO landing page — /web-design-chicopee-ma. Content in localAreas.ts.
export const prerender = true;
import LocalAreaPage from '../components/sections/LocalAreaPage.astro';
import { getLocalArea } from '../data/localAreas';
const area = getLocalArea('chicopee')!;
---
<LocalAreaPage area={area} />
