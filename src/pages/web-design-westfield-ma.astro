---
// Local SEO landing page — /web-design-westfield-ma. Content lives in
// src/data/localAreas.ts; layout in components/sections/LocalAreaPage.astro.
export const prerender = true;
import LocalAreaPage from '../components/sections/LocalAreaPage.astro';
import { getLocalArea } from '../data/localAreas';
const area = getLocalArea('westfield')!;
---
<LocalAreaPage area={area} />